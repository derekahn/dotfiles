# Machine-local secrets — copied to zsh/secrets.zsh by link.sh (gitignored).
# Fill in what you use; empty vars are harmless.

# Polygon.io / Massive market data
export POLYGON_API_KEY=""
export MASSIVE_API_KEY="$POLYGON_API_KEY"

# TradingView session cookie
export TV_SESSION_ID=""

# hedgehog (hh alias in trading.zsh)
export HEDGEHOG_API_KEY=""

# Discord webhook (youtube.zsh upload notifications)
export DISCORD_WEBHOOK_URL=""

# tinypng (tiny/tiny1/t aliases in trading.zsh)
export TINYPNG_API_KEY=""
export TINYPNG_API_KEY_2=""

# croc relay password (relay + crocr helper in network.zsh)
export CROC_PASS=""
